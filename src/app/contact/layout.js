export const metadata = {
  title: 'Contact EduLink International Campus',
  description: 'Contact EduLink International Campus for admissions, course information, fees, scholarships, student support, and general inquiries.',
  alternates: {
    canonical: '/contact',
  },
};

export default function ContactLayout({ children }) {
  return children;
}
